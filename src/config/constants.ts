const PART = 'snippet';

const TYPE = 'video';

const Q = 'programming';

const MAX_RESULTS = 20;

export const BASE_URL = 'https://www.googleapis.com/youtube/v3/search';

const AVATAR_URL =
  'https://gravatar.com/avatar/4fbf8600201a06f837c1f4cc8572b810?s=200&d=robohash&r=x';

const IMG_STYLES = {
  height: 180,
  width: '100%',
};

const AVATAR_STYLES = { width: 40, height: 40, borderRadius: 50 };

const CARD_STYLES = {
  height: 300,
  marginBottom: 14,
};

export {
  PART,
  TYPE,
  Q,
  MAX_RESULTS,
  AVATAR_URL,
  IMG_STYLES,
  AVATAR_STYLES,
  CARD_STYLES,
};
